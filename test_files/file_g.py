# Reordered code: Sum of numbers with reordered sections
def sum_numbers(nums):
    return sum(nums)

numbers = [1, 2, 3, 4, 5]
# Move this part after function definition
print(sum_numbers(numbers))  # Output the result